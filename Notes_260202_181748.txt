<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>For Nikita ❤️</title>
  <style>
    body {
      margin: 0;
      height: 100vh;
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background: linear-gradient(180deg, #ffdde8, #fff);
      display: flex;
      align-items: center;
      justify-content: center;
      overflow: hidden;
    }

    .card {
      background: white;
      padding: 30px;
      border-radius: 22px;
      width: min(420px, 90vw);
      text-align: center;
      box-shadow: 0 20px 50px rgba(0,0,0,.15);
      position: relative;
    }

    h1 {
      font-size: 28px;
      margin-bottom: 10px;
    }

    p {
      color: #555;
      margin-bottom: 25px;
    }

    .stage {
      position: relative;
      height: 220px;
    }

    button {
      border: none;
      border-radius: 999px;
      padding: 14px 22px;
      font-size: 18px;
      font-weight: 600;
      cursor: pointer;
      position: absolute;
      transition: transform .15s ease;
    }

    #yes {
      background: #ff2f7d;
      color: white;
      left: 50%;
      top: 45%;
      transform: translate(-50%, -50%);
      box-shadow: 0 10px 30px rgba(255,47,125,.4);
    }

    #no {
      background: #eee;
      color: #111;
      left: 50%;
      top: 65%;
      transform: translate(-50%, -50%);
    }

    .overlay {
      position: fixed;
      inset: 0;
      background: rgba(0,0,0,.5);
      display: none;
      align-items: center;
      justify-content: center;
    }

    .modal {
      background: white;
      padding: 30px;
      border-radius: 20px;
      text-align: center;
      width: min(420px, 90vw);
      box-shadow: 0 20px 60px rgba(0,0,0,.3);
    }
  </style>
</head>
<body>

  <div class="card">
    <h1>Nikita ❤️</h1>
    <p>Will you be my Valentine?</p>

    <div class="stage" id="stage">
      <button id="yes">YES</button>
      <button id="no">NO</button>
    </div>
  </div>

  <div class="overlay" id="overlay">
    <div class="modal">
      <h1>YAY 💖</h1>
      <p>I was hoping you’d say yes 🥹<br/>You make everything better.</p>
    </div>
  </div>

  <script>
    const noBtn = document.getElementById("no");
    const yesBtn = document.getElementById("yes");
    const stage = document.getElementById("stage");
    const overlay = document.getElementById("overlay");

    let scale = 1;

    function moveNo() {
      const s = stage.getBoundingClientRect();
      const b = noBtn.getBoundingClientRect();

      const x = Math.random() * (s.width - b.width);
      const y = Math.random() * (s.height - b.height);

      noBtn.style.left = x + "px";
      noBtn.style.top = y + "px";
      noBtn.style.transform = "translate(0,0)";

      scale += 0.1;
      yesBtn.style.transform = `translate(-50%, -50%) scale(${scale})`;
    }

    noBtn.addEventListener("mouseenter", moveNo);
    noBtn.addEventListener("touchstart", e => {
      e.preventDefault();
      moveNo();
    });

    yes